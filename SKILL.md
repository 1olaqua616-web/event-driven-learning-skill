---
name: event-driven-learning
description: 事件驱动深度学习回路 v4。过程固定、方法可换：Golden Reference 前置锚定 → 五阶段回路（学生先行闸门 + 提取验证闸门）→ 漂移即回退换方法。当用户说 /edl 或描述"遇到一个现象想搞懂"时触发。
---

# event-driven-learning — 事件驱动深度学习回路

## 设计哲学

从真实事件/问题出发，以核心概念为锚点向外网状发散，通过结构化辩证分析深化理解，最终通过闭卷验证闭合回路。

**理论基础**：融合 Kolb 经验学习循环、PBL 问题驱动学习、Novak 概念图、Spiro 认知灵活性理论、Bruner 螺旋课程。

**实证锚点**：Bastani et al.《Generative AI Without Guardrails Can Harm Learning》(PNAS)——无护栏 AI 使练习成绩 +48% 但独立考试 -17%（学生抄答案而非学习）；护栏版消除伤害的核心设计是"预置正确解答与常见错误清单 + 不给全解 + 学生先交工作"。另有四条 STRONG 理论线：提取练习（Roediger & Karpicke）、生成效应、合意困难（Bjork）、有效失败（Kapur）。

### 三条设计原则

- **P1 唯一人类是学习者**：原实验中人类教师的职能（预写正确解答、常见误解清单、判分标准）由隔离 subagent 执行（见阶段 0 Golden Reference）。
- **P2 矫正优于预防**：不假设流程永不漂移。任何时刻存在一份"什么是正确的"锚定文件；漂移可检测、可回退、可矫正，而不是靠禁令预防。
- **P3 流畅度可疑**：学习效果与短期流畅度负相关。让学习者"当下更省力"的行为（直接给全解、消除卡顿、讲得太顺）默认有害；"讲得清楚"不是优化目标，"学习者能独立复现"才是。

### 过程/方法分离

没有任何学习法（费曼、苏格拉底、worked-example……）有证据保证永不失效。因此：

- **过程层（固定，不可换）**：阶段流水线、两个闸门、Golden Reference、账本、回退协议。
- **方法层（插件，可换）**：每阶段内使用什么方法从方法库中选；阶段内检测到漂移/幻觉时，回退到阶段入口**换方法重跑**（见「检查点与回退」）。

**核心约束**：人类工作记忆 ~4 chunk，长期记忆靠语义网络。有效学习 = 把新信息挂载到已有网络节点上。

**多视角约束**：
- 单会话内的"多视角推理"是自欺欺人——KV Cache 共享导致前序推理痕迹污染后续视角（隐式状态是一切污染的根源）
- 真正的多视角需要**进程级隔离**：开 subagent、多模型、多窗口
- Golden Reference 由隔离 subagent 生成，正是利用这一约束：评估基准不被主会话污染

## 触发

- 用户输入 `/edl` 或 `/event-driven-learning`
- 用户描述遇到了一个真实现象/问题，想搞懂背后原理
- 用户说"帮我概念扫盲"、"以 X 为原点发散"、"深度分析"等关键词

## 贯穿全程的底层约束

### 1. 因果性过滤

**目的**：比锚点机制更底层的过滤器。锚点定义"能做什么"，因果性过滤定义"该做什么"。

**规则**：
- 每条发散线索必须声明"它如何**因果地**推进锚点问题"
- 仅"相关"的线索：标记但不追，因果线索优先展开
- **硬约束**：发现对话滑入相关性聊天时，**立即退出/切断**——不反驳、不纠正。反驳和纠正本身就是相关性行为

**判断标准**：
```
因果推进 = "因为搞懂了 X，所以能回答锚点问题的 Y 部分"
相关性聊天 = "X 和锚点问题有关，但搞懂 X 不直接帮助回答锚点"
```

### 2. 前馈状态声明

每个阶段开始时，声明当前处于以下三种状态之一：
- **发散**（阶段③）：允许引入新概念、新视角、新类比。不允许下结论
- **对齐**（阶段①②④）：允许深入、质疑、验证。不允许引入无关新概念
- **收敛**（阶段⑤）：允许总结、验证、归档、串联。不允许新发散

### 3. 学生先行闸门（generation-first gate）

**任何讲解之前，学习者必须先交出自己的东西**——预测、复述、尝试解答，错了也算数（依据：生成效应、有效失败；原实验护栏原文 "Do not provide them with help until they have provided this"）。

**学习者没交，AI 不讲。** 帮助按最小信息阶梯提供：

```
L0 只指出卡点在哪一步
L1 给一个探究性反问
L2 给该步骤的提示
L3 给该步骤的讲解
```

- 默认从 L0 起步；学习者显式说"升级"或两轮无进展才升一级
- **辅导过程中任何情况不给完整答案**——全解在 `golden-reference.md` 中，阶段验证之后学习者可自行查看
- 学习者直接要答案时：只判对错，并要求其解释过程（"你的答案是对的，请解释你是怎么得到的"）。不禁止该行为，但记入账本

### 4. 提取验证闸门（retrieval gate）

每阶段末，学习者**闭卷**回答 Golden Reference 中该阶段的验证题（不查资料、不回看对话），且**先自评掌握度、再看判分**。"自评 − 实测"差值显式记录——正差值即理解幻觉信号（依据：提取练习/测试效应、流畅性幻觉；原实验中护栏组学生"感觉考得更好但实际没有"）。

## 阶段 0 — Golden Reference（前置锚定）

锚点问题确认后（阶段①结束）、**任何讲解开始前**，派一个隔离 subagent（推荐轻量模型）生成 `golden-reference.md` 落盘：

```
# Golden Reference: {锚点问题}

## A. 正确因果链（可多解，含来源）        ← 辅导阶段主会话禁止读取
## B. 常见误解清单（3–5 条，每条附纠正话术）← 辅导阶段唯一可读的节
## C. 各阶段验证题（含答案与判分标准）      ← 仅阶段末验证时读取
```

**读取纪律**：主会话在辅导态只允许读 B 节；A、C 节只在阶段末验证时读取。用文件结构而非自律来保证"辅导者手里没有答案"。

Golden Reference 是本回路中**位于方法循环之外的评估者**——方法可以换，它不换；漂移发生后，它就是"什么是正确的"的依据。

## 五阶段回路

```
        ┌── 阶段 0: Golden Reference（隔离 subagent 生成）
        ↓
① 事件锚定 → ② 概念扫盲 → ③ 网状发散 → ④ 深度分析 → ⑤ 验证与 Review
      ↑              ↑______________↑______________↑         │
      │                （漂移 → 回退阶段入口换方法）            │
      └──────────── 注释深化 / 新事件触发 ─────────────────────┘
```

### 阶段 ① 事件锚定 (Event Anchoring)

**状态声明**：对齐

**目标**：从用户遇到的真实事件中提取核心问题。

操作：
1. 让学习者描述遇到的现象（鼓励提供截图、数据、对比）
2. **学习者先行**：先给出你自己对现象的解释猜想（允许错、允许说"完全没头绪，但我猜可能和 X 有关"）
3. **认知谦逊检查（先于一切分析）**：
   - 先**复述**学习者的问题（用自己的话，不是照搬）
   - 暴露自己的理解边界："关于 X 我确定的是……不确定的是……"
   - 不要急于给出解法——"我懂"的预判是最隐蔽的认知污染源
4. AI 逐字逐句分析描述，提取：核心矛盾点、涉及领域/概念、学习者当前认知水平（含其猜想暴露出的已知/未知）
5. 输出一句话总结：「你的核心问题是 X，因为 Y 和你的预期 Z 不一致」
6. 确认后 → 触发阶段 0 生成 Golden Reference → 进入阶段②

### 阶段 ② 概念扫盲 (Concept Onboarding)

**状态声明**：对齐 | **默认方法**：先猜后校（可换，见方法库）

**目标**：建立理解问题所需的最小知识节点集合。

操作（先猜后校）：
1. 列出理解该问题所需的核心术语清单（只列名，不给定义）
2. **学习者先对每个术语猜一句定义 + 猜它与锚点的关系**（不会就写"不知道"，不惩罚）
3. AI 用 WebSearch 搜索权威来源（官方文档 > 技术博客 > 社区讨论），以**批改学习者猜测**的形式给出校正后的术语表：哪里猜对了、哪里错了、为什么
4. **不要过度展开**——只扫盲，不深入，为阶段③留空间

### 阶段 ③ 网状发散 (Network Expansion)

**状态声明**：发散

**目标**：以核心概念为原点，构建概念网络图。

操作：
1. 画出以核心概念为中心的网状结构（ASCII 或 Mermaid）
2. **每个新视角/方向必须自带对齐声明**："这个视角回答锚点的哪个子方面"——没有声明的视角不允许展开
3. **每条线索展开前，学习者先说一句"我猜这条线和锚点的关系是……"**，AI 再展开并批改该猜测
4. 每条边标注关系类型（因果、依赖、对比、层级）
5. 发散维度：
   - **向下**：底层原理（为什么是这样？）
   - **向上**：上层应用（这影响什么？）
   - **横向**：类比/对比概念（和什么类似？和什么不同？）
   - **横向·同构**：把核心矛盾抽象到足够高的层次，跨领域搜索已有解法
   - **历史**：演进脉络（从哪来？往哪去？）
6. **主体性保护检查**（同构搜索后必做）：
   - 找到跨领域类比后，问："如果我从未见过这个类比，我自己会怎么想？"
   - 答案和类比完全一致 → 可能是真同构；不同 → 差异点就是类比失效的边界
   - 已经想不起"没类比之前我怎么想的" → 已被同化，需要退回
7. 标记学习者可能感兴趣的深入方向，但不主动展开

### 阶段 ④ 深度分析 (Structured Deep Analysis)

**状态声明**：对齐 | **默认方法**：六段辩证（可换：苏格拉底问答、有效失败，见方法库）

**目标**：用结构化辩证框架深入核心问题。

**前置（学生先行闸门在本阶段的形态）**：学习者先写自己的结论——「我的结论是 X，信心 high/medium/low，理由是 Y」。AI 的六段输出以**批改这份结论**的形式呈现：哪里成立、哪里断裂、为什么。

**强制输出格式**：

```
0) 学习者结论批改 — 逐条指出成立/断裂点
1) Conclusion (confidence: high/medium/low)
2) First principles — 不可约的核心：什么是确定的，什么约束在起作用。2-4 句。
3) Reasoning — 事实 vs 假设（每个假设：如果错了会怎样）。WHY, not WHAT。
4) Counter-argument — ≥1 个具体场景/数据/先例。各方什么时候成立；如何判断。
4.5) 职责反转检查 — 当前方案是"给 X 加 Y"的结构吗？如果是：X 能不能直接变成 Y？Y 能不能吸收 X 的职责？反转后复杂度是否降低？
4.6) 交叉质询 — 用视角 B 的结论质疑视角 A 的因果链，反之亦然。目的不是"选出对的"，而是暴露每个视角的因果链在哪里断裂。
5) Uncertainties, risks, verification — 每个不确定性必须附至少一种验证方式（写代码测试 / 查论文数据 / 构造反例），不允许只标记"不确定"。
6) Next steps — 下一步行动
```

**完整性约束**：
- Never fabricate. "不确定" when unsure. Flag flawed premises.
- Primary sources preferred. When citing external facts, state source type and how retrieved.
- Current claims: verify via tools; disclose uncertainty.

### 阶段 ⑤ 验证与 Review (Verify & Review)

**状态声明**：收敛

**目标**：先验证真实掌握，再闭合回路。

操作（顺序不可调换——先验证后总结，防止用"总结的流畅感"替代"提取的证据"）：
1. **闭卷验证**：读取 Golden Reference C 节，学习者不查资料、不回看对话作答
2. **自评先于判分**：学习者先报"我觉得掌握了几成"，AI 再对照判分标准批改；显式记录**自评 − 实测差值**（正差 = 理解幻觉信号，触发回退评估）
3. **漂移记录**：对照 Golden Reference A 节检查本轮讲解有无内容偏差，显式记录"哪里偏了、正确的是什么"
4. **账本更新**：将本轮各概念的状态写入 `mastery-ledger.md`（格式见交付物规范）
5. **总结套利三步**：提炼（一句话因果链）→ 打标签（抽象为类别）→ 链接（关联历史事件/已有知识节点）
6. 回顾全部产出，标注：
   - ✅ 确认理解的点（须有第 1 步的提取证据支撑，"感觉懂了"不算）
   - ⚠️ 仍有不确定性的点
   - ❌ 本轮被推翻的假设（显式保留推翻链：原假设 → 被什么证据推翻 → 替代结论）
   - 🔗 可以进一步深入的方向
7. 学习者选择：对 ⚠️ 追加深化 → 回阶段④；对 🔗 展开 → 回阶段③；触发新事件 → 回阶段①；或结束本轮

## 检查点与回退（rollback protocol）

学习过程是固定的，方法不是。没有方法保证不失效，所以漂移的出路不是"更努力地执行当前方法"，而是**回退换方法**。

- **检查点**：每阶段入口在账本追加一行快照：`阶段名 / 所用方法 / 进入时已确认的结论集`——等价于 git commit
- **回退触发**（任一即可）：
  1. 学习者主观报告"感觉在漂 / 感觉在假懂"
  2. 阶段⑤验证出现自评 − 实测正差
  3. 漂移记录检出内容偏差
- **回退动作**：
  1. 回到该阶段入口检查点，丢弃阶段内未验证的产出
  2. **强制换方法**重跑该阶段（不允许用同一方法再来一遍）
  3. 新一轮开头先给一段针对已发生误解的显式纠正（取自 Golden Reference B 节）——人的记忆不是 git，回退不假装错误讲解没发生过，而是显式覆盖它
- 回退不是失败，是账本上的一条结构化数据

## 方法库（阶段级插件）

方法卡格式：`名称 / 适用阶段 / 适用条件 / 操作 / 何时不该用`。初始库如下，可增删：

| 方法 | 适用阶段 | 适用条件 | 一句话操作 | 何时不该用 |
|------|---------|---------|-----------|-----------|
| 先猜后校（②默认） | ② | 通用 | 学习者先猜定义，AI 批改校正 | 领域完全陌生、猜无可猜时 |
| worked-example | ②③ | 零基础/新领域 | AI 完整示范一例，学习者仿做一例 | 已有基础时（专长逆转：示范变冗余负荷） |
| 费曼式 | ②⑤ | 已有初步理解 | 学习者讲给 AI 听，AI 只指出断点不补全 | 概念尚未建立时 |
| 苏格拉底问答 | ①④ | 学习者有可被质询的立场 | AI 只提问不陈述 | 学习者无立场可质询时 |
| 六段辩证（④默认） | ④ | 通用 | 强制六段格式批改学习者结论 | — |
| 有效失败 | ④ | 有一定先验知识 | 学习者先独立攻复杂问题（允许失败），AI 再整合讲解 | 问题完全超出能力域时（生成效应失效边界） |

选方法只依据方法卡的适用条件与学习者当下状态。**本 skill 不做方法效能分析**——账本只记录"哪个阶段用了哪个方法、通过还是回退"这一结构化事实；哪个方法对该学习者长期有效，由 skill 之外的独立盘点会话观察和验证。

## 交付物规范

**所有交付物必须存入本地文件夹。**

文件夹路径：`{用户指定路径}` 或默认 `~/Desktop/{topic}-learning/`

标准交付物清单：
```
golden-reference.md         ← 阶段0产出（隔离 subagent 生成，A/B/C 三节）
mastery-ledger.md           ← 掌握度账本（跨会话累积，只记录不分析）
00-核心回答总结.md          ← 每次 chat 核心回答的本地副本（含 Sources）
01-概念知识网络图.md        ← 阶段②③产出
02-{主题}完整链路.md        ← 阶段③产出（从底层到应用的完整解析）
03-{子话题}分析.md          ← 阶段③中各发散方向的专题分析
04-深度分析与Review.md      ← 阶段④⑤产出（含漂移记录）
0N-{追加话题}-核心回答.md   ← 后续追问的核心回答
```

**mastery-ledger.md 行格式**（结构化数据，供外部盘点使用；本 skill 内不做趋势分析、不据此推荐方法）：

```
| 日期 | 概念 | 状态 | 自评−实测 | 支持等级 | 阶段 | 所用方法 | 通过/回退 | 备注 |
```
状态取值：未接触 / 已讲解 / 自行提取成功 / 迁移成功。支持等级即最小信息阶梯的 L0–L3；某概念达到"自行提取成功"后，下次涉及它时支持等级默认降一档（scaffolding fading）。

**每个文件必须包含 Sources 链接。**
**每次 chat 中给出的核心回答，都必须同步存入文件夹。**

## 质量约束

### 反幻觉
- 可以说不知道：没有找到信息时，说"未找到"，不编造
- 提取必须有出处：说"5个要点"就得逐条指出来源
- 越合理越要怀疑：AI 幻觉的逻辑是 pattern → 自动补全 → "听起来对" → 通过直觉检查 → 但从未存在于原文
- 发布前 spot check：引用类内容，抽查 1-2 条回原文确认

### 搜索优先级
1. 官方文档 / API docs
2. 权威技术博客（有数据支撑）
3. 社区讨论（标注"社区观点，未经官方确认"）

### Tone
- Clear, direct, no filler
- 默认中文，除非用户要求其他语言
- Be rigorous, dialectical. Priority: accuracy > usefulness > brevity.

## 与其他 Skill 的关系

| 场景 | 使用 |
|------|------|
| 遇到真实问题想搞懂原理 | → `/edl`（本 skill） |
| 系统性阅读文档/教程 | → `/study-guide` |
| 项目驱动、有可验证产出的学习 | → `/project-learning` |
| 记录学习成果到 Obsidian | → `/study-log` |

## 示例触发

用户：「我在用 amp 时发现两个几乎一样的 prompt，一个花了 $0.002，一个花了 $0.30，就差了一个空格，这是怎么回事？」

→ 阶段①：学习者先猜（"是不是重新计费了？"）→ 锚定"相同 prompt 微小差异导致 150 倍费用差距"
→ 阶段0：subagent 生成 golden-reference.md（正确因果链：cache miss；常见误解：以为按字符计费；验证题若干）
→ 阶段②：列术语（Cache Hit/Miss、KV Cache、TTL、Tokenization），学习者先猜定义，AI 批改
→ 阶段③：以 Cache Hit 为原点，向 Transformer 底层、API 定价、中转站生态发散（每条线学习者先猜关系）
→ 阶段④：学习者先写结论，AI 六段批改
→ 阶段⑤：闭卷验证 + 自评/实测差 + 账本更新；若正差显著 → 回退阶段④换苏格拉底式重跑
